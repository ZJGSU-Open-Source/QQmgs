@using Twitter.App.Models.ViewModels
@model LoginViewModel
@{
    ViewBag.Title = "登陆";
    Layout = "~/Views/Shared/_Login_Layout.cshtml";
}

<div class="index-main" style="position: relative; z-index: 1">
    <div class="index-main-body">
        <div class="index-header">
            @*<h1 class="logo hide-text">知乎</h1>*@
            <h1 style="font-size: 60px; margin-top: 0px">全球某工商</h1>
            <h2 class="subtitle">连接每一位商大师生</h2>
        </div>
        <div class="desk-front sign-flow clearfix sign-flow-simple">
            <div class="index-tab-navs">
                <div class="navs-slider" data-active-index="1">
                    <a href="@Url.Action("Register")">注册</a>
                    <a href="#!" class="active">登录</a>
                    <span class="navs-slider-bar"></span>
                </div>
            </div>
            <div class="view view-signin selected">
                @using (Html.BeginForm("Login", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, string.Empty, new { @class = "text-danger" })

                    <form>
                        <div class="group-inputs">
                            <div class="input-field" style="margin-top: 0px">
                                <input name="UserName" placeholder="手机号" id="first_name" type="text" style="margin-bottom: 0px; width: 95%; padding-left: 5%" class="validate">
                            </div>
                            <div class="input-field" style="margin-top: 0px">
                                <input name="Password" placeholder="密码" id="password" type="password" style="margin-bottom: 0px; border-bottom: none; width: 95%; padding-left: 5%" class="validate">
                            </div>
                        </div>
                        <div class="button-wrapper command">
                            <button class="sign-button submit" type="submit">登录</button>
                        </div>
                        <div class="signin-misc-wrapper clearfix">
                            <label class="remember-me">
                                <input name="RememberMe" type="checkbox" id="remember-me-input" checked="checked" />
                                <label for="remember-me-input" style="font-size: 12px">记住我</label>
                            </label>
                            <a class="unable-login" href="@Url.Action("ForgotPassword")">忘记密码?</a>
                        </div>
                    </form>
                }
                <div class="QRCode">
                    <button class="QRCode-toggleButton" onclick="location.href = '@Url.Action("About", "Home")'" type="button">
                        <span class="sprite-global-icon-qrcode"></span>
                        <span class="QRCode-toggleButtonText ">点击了解全球某工商.com</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
